# Platform CADC/CANFAR Python clients (/opt/astroai/venv/cadc).
# Unpinned — uv resolves at image build; upgrade in-session via upgrade-cadc-tools.sh.
cadcdata
cadctap
vos
canfar
# canfar-lab installed from vendor wheel in Dockerfile (same venv).
